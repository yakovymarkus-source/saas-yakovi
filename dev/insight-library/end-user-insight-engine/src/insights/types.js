'use strict';

const SEVERITIES = ['low', 'medium', 'high', 'critical'];
const USER_LEVELS = ['beginner', 'intermediate', 'advanced'];
const DISPLAY_MODES = ['simple', 'professional'];
const BUSINESS_TYPES = ['services', 'ecommerce', 'lead_generation', 'generic'];

module.exports = {
  SEVERITIES,
  USER_LEVELS,
  DISPLAY_MODES,
  BUSINESS_TYPES,
};
